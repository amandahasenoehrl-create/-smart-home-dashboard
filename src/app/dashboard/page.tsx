export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Dashboard functionality will be available when the full app is deployed.</p>
      <a href="/">← Back to Home</a>
    </div>
  );
}